package board.cells.impl.notwalkable.impl;

import board.cells.Position;
import board.cells.impl.notwalkable.NotWalkableCell;

public class NotPlayableCellImpl extends NotWalkableCell
{

	public NotPlayableCellImpl(Position pos)
	{
		super(pos);
	}
}
